// "use client";

import React from "react";

const Page = ({ children }: { children: JSX.Element }) => {
    return (
        <>
            {/* {...} */}
            {/* {children} */}
        </>
    );
};

export default Page;
